source 'https://rubygems.org'

# Specify cocoapods via bundler for consistent pod install execution
# Using bundle exec ensures path resolution is correct
gem 'cocoapods', '~> 1.15.0'
gem 'ffi', '1.15.5'  # Pin ffi to avoid Ruby 3.0+ requirement
